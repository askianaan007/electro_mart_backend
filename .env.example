DATABASE_URL=
DIRECT_URL=

JWT_SECRET=
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN_DAYS=30

PORT=3000

# Comma-separated list of allowed frontend origins, e.g.:
# CORS_ORIGIN=https://app.electromart.com,https://admin.electromart.com
# Leave unset only for local development (allows all origins).
CORS_ORIGIN=

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Electro Mart <no-reply@electromart.com>"
